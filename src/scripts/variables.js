import path from 'path';

export const ROOT_PATH     = path.join(__dirname, '../../');
export const RESOURCE_PATH = path.join(ROOT_PATH, './src/theme');
export const DISTRICT_PATH = path.join(ROOT_PATH, './dist/theme');
export const COVERAGE_PATH = path.join(ROOT_PATH, './coverage/theme');
export const SERVER_PORT   = 51000;
